# soroban-initializable-trait
This repository demonstrates a shared Initializable trait for Soroban contracts to eliminate duplicated initialization checks. It provides consistent flag storage, guard logic against re-initialization, and migration examples for existing contracts. Strong focus on code reuse and safety in Stellar smart contracts. 
